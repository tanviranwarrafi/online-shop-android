<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@color/colorPrimary"
    android:orientation="vertical"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/navHeader_userProfileImg"
        android:layout_width="60dp"
        android:layout_height="65dp"
        android:contentDescription="@string/nav_header_desc"
        android:elevation="3dp"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        app:layout_constraintBottom_toTopOf="@+id/navHeader_signingStatus"
        app:layout_constraintStart_toStartOf="parent"
        app:srcCompat="@mipmap/user_round" />

    <TextView
        android:id="@+id/navHeader_signingStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:text="Not Signed In"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textColor="@color/colorAccent"
        app:layout_constraintBottom_toTopOf="@+id/navHeader_emailAddress"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/navHeader_emailAddress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Email Adderss"
        android:textColor="#80ffffff"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageView
        android:id="@+id/navHeader_addUserProfileImg"
        android:layout_width="19dp"
        android:layout_height="23dp"
        android:elevation="5dp"
        app:layout_constraintBottom_toBottomOf="@+id/navHeader_userProfileImg"
        app:layout_constraintEnd_toEndOf="@+id/navHeader_userProfileImg"
        app:srcCompat="@mipmap/add_icon" />

</androidx.constraintlayout.widget.ConstraintLayout>
